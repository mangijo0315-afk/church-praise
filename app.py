import streamlit as st

st.set_page_config(page_title="대흥교회 찬양팀", layout="centered")
st.title("⛪ 대흥교회 찬양팀 소통판")

# 역할 선택
user_type = st.sidebar.radio("역할 선택", ["인도자", "반주자"])

# 간단한 신호 전달 (현재는 연습용)
if user_type == "인도자":
    st.header("🎮 인도자 컨트롤러")
    if st.button("🔄 후렴 반복"):
        st.success("반주자에게 '후렴 반복' 신호를 보냈습니다!")
    if st.button("⏩ 2절로 점프"):
        st.warning("반주자에게 '2절 점프' 신호를 보냈습니다!")
else:
    st.header("🎹 반주자 모니터")
    st.info("인도자의 신호를 기다리는 중입니다...")
